import styled from '@emotion/styled'

export const WorksInfoBlock = styled.section`
    width: 100%;
    height: auto;
    padding: 8rem 0;
    background-image: linear-gradient(rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.4)),url(works-bcg.jpg);
    background-size: cover;
    background-position: center;
    position: relative;
`
